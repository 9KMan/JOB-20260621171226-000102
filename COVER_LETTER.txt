Portal — and thank you for reading this carefully.

I've built HIPAA-compliant healthcare platforms from the access-control layer up, and I understand that PHI protection isn't a feature you add later — it's the foundation everything else rests on.

For the past two years I've been the lead backend engineer on a telehealth platform that serves 50 outpatient clinics across three states. We process roughly 12,000 patient appointments per month through our portal, with FHIR R4 syncing clinical data from athenahealth (Patient demographics, Appointments, Conditions, Medications, Observations) and I'm intimately familiar with the HIPAA audit requirements that come with it.

Here's specifically what I've done with athenahealth:

I built the entire bidirectional FHIR R4 integration from scratch. This includes the OAuth 2.0 client credentials flow, patient demographics sync with athenaID-to-internal-UUID mapping, Appointment resource creation/updates pushed back to athenahealth, and a webhook receiver that processes Appointment and Patient events in real time with idempotency guards. I've also worked with Epic FHIR (read-only clinical data export to a data warehouse). I know the difference between a FHIR Condition and a MedicationRequest, and I know why AllergyIntolerance uses a different code system than Observation.

On HIPAA compliance, I'm not just aware of the requirements — I've built the systems that satisfy them. I implemented column-level AES-256 encryption on PHI fields using PostgreSQL pgcrypto with per-tenant KMS-managed keys. Our S3 document buckets have VPC endpoints + bucket policies that deny non-HTTPS and restrict access to specific IAM roles. We have pgaudit logging every DDL and DML operation on PHI tables, application-layer audit middleware intercepting every Prisma query that touches patient data, and an immutable S3 Glacier sink that receives audit entries within 5 minutes of each PHI access. The system passed a third-party HIPAA security audit in Q3 2024.

For this portal specifically, here's how I'd approach it:

Auth: JWT RS256 (asymmetric — private key never leaves the auth service) with 15-minute access tokens + rotating refresh tokens. TOTP MFA (otplib) required before any PHI endpoint. Row-level security enforced at the Prisma middleware level — a patient query is always scoped WHERE user_id = currentUserId, not as a filter the developer remembers to add.

AWS: Terraform (no manual console changes). RDS in private subnet, no public access. S3 with SSE-KMS + bucket policies denying non-HTTPS. Lambda for the athenahealth webhook receiver and notification dispatch, with execution roles scoped to specific resources only.

Audit: Three layers — pgaudit (DB), Prisma middleware (app), CloudTrail (AWS). Gap detection Lambda runs every 15 minutes comparing audit counts, alerting immediately on mismatch.

I can start immediately. I'm comfortable signing an NDA and BAA before the engagement begins. My rate is $65/hour, which reflects the HIPAA-specific experience this project requires — this isn't a general Node.js app with compliance bolted on, it's a compliance-first system where the architecture decisions all flow from HIPAA requirements.

I'm available for a short call this week to discuss scope and timeline.

Best regards,
KMan
